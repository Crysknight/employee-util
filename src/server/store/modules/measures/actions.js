// import {
    // getMeasures,
    // createMeasure,
    // deleteMeasures
// } from '$models';
// import {
    // MUTATION_SET_MEASURES,
    // MUTATION_CREATE_MEASURE,
    // MUTATION_DELETE_MEASURES
// } from '$shared/constants';

export default {
    // async getMeasures({ commit }) {
    //     const measures = await getMeasures();

    //     commit(MUTATION_SET_MEASURES, measures);
    // },
    // async createMeasure({ commit }, { data: name }) {
    //     const measure = await createMeasure(name);

    //     commit(MUTATION_CREATE_MEASURE, measure);
    //     commit(`employees/${MUTATION_CREATE_MEASURE}`, measure, { root: true });
    // },
    // async deleteMeasures({ commit }, { data: measuresIds }) {
    //     await deleteMeasures(measuresIds);

    //     commit(MUTATION_DELETE_MEASURES, measuresIds);
    //     commit(`employees/${MUTATION_DELETE_MEASURES}`, measuresIds, { root: true });
    // }
};
